function calculaPrecoTotal(quantidade) {
 const custoCompra
 const quantidade = 5
 const preco1 = 1.30
 const preco2 = 1
  
  if{
    quantidade >= 12
       custoCompra = quantidade*preco2
  }else{
    custoCompra = quantidade*preco1
  }
  
}